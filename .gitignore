# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv / venv
.venv/

# Secrets — never commit real env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Generated skill / workflow output (runtime artifacts, not source)
output/

# Large raw datasets (download/regenerate, don't commit binaries)
frontend/data/
*.h5ad

# Runtime knowledge-graph store (mutates each run; curated fixtures are demo_data/)
skills/virtual-biotech-cso/kg.json

# scanpy dataset cache
data/*.h5ad

# runtime knowledge-graph store (regenerated by frontend/server.py)
frontend/kg.json

# macOS
.DS_Store
